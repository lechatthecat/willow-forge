use anyhow::Result;

pub async fn execute() -> Result<()> {
    println!("🌿 Starting Willow development server...");
    println!("This command should be run from within a Willow application directory.");
    println!("Use 'cargo run' to start your application instead.");
    Ok(())
}
